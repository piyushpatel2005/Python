# Ecommerce web application Python Django

```shell
python3 -m venv ./venv
source ./venv/bin/activate
pip install Django==1.11.4
pip freeze
mkdir ecommerce && cd ecommerce
django-admin startproject ecommerce
```